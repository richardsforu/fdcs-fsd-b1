package com;

public class Notes {
	/*
	 * 	Final keyword
	 * ------------------
	 * 	3 ways can be used
	 * 
	 * 	-> if the final keyword is used before a variable, then that 
	 *     variable becomes const
	 *     
	 *  -> if the final keyword is used before a method, then that 
	 *     method can not be overridden in the sub classes
	 *  
	 *  -> if the final keyword is used before a class, then that class
	 *     can not be extended by any other classes
	 * 
	 * 
	 */

}
