package com;

public class Notes {
	/*
	 * Notes
	 * ---------
	 * 	-> If super class method is public , then sub class overridden methods 
	 *     can have only public
	 *  
	 *  -> If super class method is default, then sub class overridden method
	 *     can be default, public , protected
	 *     
	 *  -> If the super class method is protected, then sub class overridden method
	 *     can be protected , public 
	 *     
	 *  
	 */

}
